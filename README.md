# Neovim configuration

## Requirements

- Neovim `0.12+`
- `tree-sitter` CLI `0.26+` available on `PATH`

## Commands

- To format lua files, run `stylua .`
- On a new machine, run `:TSInstallConfigured`
- After upgrading `nvim-treesitter`, run `:TSUpdate`
